CREATE INDEX IF NOT EXISTS {table}_geom_idx
    ON ONLY {schema}.{table} USING gist
    (geom);
CREATE INDEX IF NOT EXISTS {table}_geom_wm_idx
    ON ONLY {schema}.{table} USING gist
    (geom_wm);
CREATE INDEX IF NOT EXISTS {table}_alert__date_idx
    ON ONLY {schema}.{table} USING btree
    (alert__date);